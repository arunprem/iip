<?xml version="1.0" encoding="utf-8"?>
<!-- Dev: allow HTTP to local IAM backend (physical device / emulator). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
